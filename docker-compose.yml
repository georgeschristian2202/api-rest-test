services:
  api:
    build:
      context: .
    container_name: api-express-v2
    ports:
      - "5000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
